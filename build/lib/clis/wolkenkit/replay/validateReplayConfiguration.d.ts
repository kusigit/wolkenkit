import { Application } from '../../../common/application/Application';
declare const validateReplayConfiguration: ({ application, replayConfiguration }: {
    application: Application;
    replayConfiguration: any;
}) => void;
export { validateReplayConfiguration };
